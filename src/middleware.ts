export const config = {
  matcher: ["/audiobook", "/podcast", "/jobs", "/settings", "/voices"],
};

export default function middleware() {
  // Placeholder for auth gate (Supabase Auth to be added).
}
